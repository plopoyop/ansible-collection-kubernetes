---
apiVersion: traefik.io/v1alpha1
kind: MiddlewareTCP
metadata:
  name: "{{ current_traefik_middleware.name }}"
{% if current_traefik_middleware.spec is defined %}
spec:
{{ current_traefik_middleware.spec | to_nice_yaml(indent=2) | indent(2, true) }}
{% endif %}
